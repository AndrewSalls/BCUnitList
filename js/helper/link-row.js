export function observeRowChange(row,observerCallback){row.querySelector(".unit-icon").addEventListener("click",observerCallback),row.querySelectorAll(".level-select").forEach((l=>l.addEventListener("change",observerCallback))),row.querySelectorAll(".talent-box .talent-level").forEach((t=>t.addEventListener("change",observerCallback))),row.querySelectorAll(".orb-selector").forEach((o=>o.addEventListener("change",observerCallback))),row.querySelector(".fav-icon").addEventListener("click",observerCallback),row.querySelectorAll(".row-option .option-button").forEach((o=>o.addEventListener("click",observerCallback))),row.addEventListener("generic-update",observerCallback)}export function getValuesFromRow(row){const output={id:parseInt(row.querySelector(".row-id").textContent),current_form:parseInt(row.querySelector(".row-image").getAttribute("data-form")),level:parseInt(row.querySelector(".max-level.level-select").value),plus_level:0,talents:[],ultra_talents:[],orb:[],favorited:"1"===row.querySelector(".row-favorite .fav-wrapper").getAttribute("data-favorited"),hidden:row.classList.contains("hidden")},plusLevel=row.querySelector(".max-plus-level.level-select");plusLevel&&(output.plus_level=parseInt(plusLevel.value));const talents=[...row.querySelectorAll(".talent-box.regular-talent")];talents.length>0&&(output.talents=talents.map((t=>parseInt(t.querySelector(".talent-level").innerText))));const ultraTalents=[...row.querySelectorAll(".talent-box.ultra-talent")];ultraTalents.length>0&&(output.ultra_talents=ultraTalents.map((t=>parseInt(t.querySelector(".talent-level").innerText))));const orbs=[...row.querySelectorAll(".orb-selector")];return orbs.length>0&&(output.orb=orbs.map((o=>o.querySelector(".orb-color").dataset.trait?{trait:o.querySelector(".orb-color").dataset.trait,type:o.querySelector(".orb-type").dataset.type,rank:o.querySelector(".orb-rank").dataset.rank}:null))),output}